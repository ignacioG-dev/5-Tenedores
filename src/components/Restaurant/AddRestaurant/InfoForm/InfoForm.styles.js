import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    content: {
        marginHorizontal: 10,
        marginTop: 20,
    },

    textArea: {
        height: 100,
        width: "100%",
        padding: 0,
        margin: 0,
    },
});
